<h1>
  <% if(!currentUser){ %> Hi Guest <% } else { %> Hi <%= currentUser.name %> <%
  } %>
</h1>

<form action="/logout?_method=DELETE" method="POST">
  <button class="btn btn-sm btn-primary">Log Out</button>
</form>
